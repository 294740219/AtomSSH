# AtomSSH 会话管理页

> 文档状态：阶段性冻结
>
> 冻结时间：2026-06-16
>
> 冻结范围：当前文档定义的会话管理页职责和第一版交互
>
> 变更规则：调整会话管理页时必须同步更新本文档。

会话管理页负责 profile 列表、搜索、分组、新增、编辑、删除、复制、导入导出入口。

允许：

- 展示 profile 摘要。
- 启动连接测试。
- 打开新增 / 编辑弹窗。
- 删除前确认。

禁止：

- 不得在页面中直接创建 SSH client。
- 不得在列表行中保存凭据明文。
- 不得绕过 Application 修改仓储。
