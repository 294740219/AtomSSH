# AtomSSH SFTP 浏览器

> 文档状态：阶段性冻结
>
> 冻结时间：2026-06-16
>
> 冻结范围：当前文档定义的 SFTP 浏览器职责和交互边界
>
> 变更规则：调整 SFTP 页面时必须同步更新本文档。

SFTP 浏览器用于当前 SSH profile 或活动会话的远程文件管理。

第一版能力：

- 列目录。
- 返回上级。
- 刷新。
- 创建目录。
- 删除。
- 重命名。
- 上传。
- 下载。

上传和下载只创建传输任务，不在 UI 中直接执行文件传输。

SFTP 页面显示的是远程对象快照，不持有 SFTP client。
