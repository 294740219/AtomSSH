# AtomSSH 设置页

> 文档状态：阶段性冻结
>
> 冻结时间：2026-06-16
>
> 冻结范围：当前文档定义的设置页职责和第一版边界
>
> 变更规则：调整设置页时必须同步更新本文档。

设置页包含：

- 终端默认设置。
- 外观和主题。
- 日志目录和诊断。
- 配置目录。
- 默认传输行为。

设置保存必须通过 Application 用例触发。

凭据与安全、known hosts 和导入导出有独立页面；设置页可以提供入口，但不承载这些流程的主要表单。
