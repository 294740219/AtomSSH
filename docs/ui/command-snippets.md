# AtomSSH 命令片段页

> 文档状态：阶段性冻结
>
> 冻结时间：2026-06-16
>
> 冻结范围：当前文档定义的命令片段页职责和第一版边界
>
> 变更规则：调整命令片段能力时必须同步更新本文档。

命令片段用于保存常用命令并发送到当前活动终端。

第一版能力：

- 创建、编辑、删除命令片段。
- 按分组展示。
- 发送到当前活动终端。

第一版不做宏录制、多会话广播和脚本引擎。

发送命令必须通过 Application 用例进入终端会话边界，不直接访问协议库 channel。
